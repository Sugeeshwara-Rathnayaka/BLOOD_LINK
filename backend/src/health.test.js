// A simple dummy test to verify our CI/CD pipeline works!
describe("Blood-Link Initial Setup", () => {
  it("should prove the testing suite is functioning perfectly", () => {
    expect(1 + 1).toBe(2);
  });
});
